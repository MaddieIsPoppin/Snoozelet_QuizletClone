"use client";
import { useEffect, useMemo, useState } from "react";
const instructions={teach:"Act as an interactive tutor. Teach this material starting with my weakest concepts. Ask questions throughout and explain misunderstandings simply and then technically.",quiz:"Quiz me one question at a time. Do not reveal an answer until I respond. Adapt difficulty and explain mistakes clearly.",weak:"Explain my weak areas interactively. Find the likely misunderstanding, use a simple example, then ask me to explain it back.",test:"Prepare me for a test. Identify gaps, teach the highest-priority weaknesses, then run a realistic mixed test.",check:"Check my understanding using probing questions. Identify misconceptions and help me correct them.",improve:"Review these flashcards for ambiguity and poor active-recall design. Return improved cards using the # SNOOZELET Q:/A: format.",create:"Identify important gaps and create non-duplicate cards. Return only cards in the # SNOOZELET Q:/A: format."};
export function buildStudyContext({subject,unit,decks=[],cards=[]}){const weak=cards.filter(c=>c.weak||(c.attempts&&c.accuracy<60));const reviewed=cards.filter(c=>c.attempts);const accuracy=reviewed.length?Math.round(reviewed.reduce((s,c)=>s+c.accuracy,0)/reviewed.length):0;const lines=[`Module: ${subject||"Unorganised"}`];if(unit)lines.push(`Study Unit: ${unit}`);lines.push(`Decks: ${decks.map(d=>d.title).join(", ")||"None"}`,"","Current progress:",`Cards: ${cards.length}`,`Overall accuracy: ${accuracy}%`,`Due: ${cards.filter(c=>c.due).length}`,`Weak concepts: ${weak.map(c=>c.term).slice(0,20).join(", ")||"None identified yet"}`);const selected=weak.length?weak:cards;if(selected.length)lines.push("",weak.length?"Weak cards:":"Study material:",...selected.slice(0,100).flatMap(c=>["",`Q: ${c.term}`,`A: ${c.definition}`,c.attempts?`Accuracy: ${c.accuracy}%`:"Not studied yet"]));return lines.join("\n");}
export default function StudyContextTools({subject,unit="",decks=[],cards=[],initialProvider="chatgpt"}){const context=useMemo(()=>buildStudyContext({subject,unit,decks,cards}),[subject,unit,decks,cards]);const[mode,setMode]=useState("teach");const[provider,setProvider]=useState(initialProvider==="gemini"?"gemini":"chatgpt");const generated=`${instructions[mode]}\n\n${context}`;const[output,setOutput]=useState(generated);const[copied,setCopied]=useState(false);useEffect(()=>setOutput(generated),[generated]);async function copy(){try{await navigator.clipboard.writeText(output);}catch{const a=document.createElement("textarea");a.value=output;document.body.append(a);a.select();document.execCommand("copy");a.remove();}setCopied(true);window.setTimeout(()=>setCopied(false),1800);}async function launch(){const tab=window.open("about:blank","_blank");await copy();const url=provider==="gemini"?"https://gemini.google.com/app":"https://chatgpt.com/";if(tab)tab.location.href=url;else window.open(url,"_blank","noopener,noreferrer");}const options=[["teach","Teach me this"],["quiz","Quiz me"],["weak","Explain weak areas"],["test","Prepare for a test"],["check","Check my understanding"],["improve","Improve flashcards"],["create","Create more flashcards"]];return <section className="context-tools ai-study-tools"><div className="ai-study-intro"><div className="ai-help-mark">AI</div><div><p className="eyebrow">AI Help</p><h2>Open a focused AI study room</h2><p>Snoozelet prepares the right deck context and copies it when your chosen tutor opens.</p></div></div><div className="ai-provider-picker" aria-label="Choose AI tutor"><button className={provider==="chatgpt"?"active":""} type="button" onClick={()=>setProvider("chatgpt")}><strong>ChatGPT</strong><small>OpenAI tutor</small></button><button className={provider==="gemini"?"active":""} type="button" onClick={()=>setProvider("gemini")}><strong>Google Gemini</strong><small>Gemini tutor</small></button></div><div className="ai-action-grid">{options.map(([v,l])=><button className={mode===v?"active":""} type="button" onClick={()=>setMode(v)} key={v}>{l}</button>)}</div><textarea value={output} onChange={e=>setOutput(e.target.value)} rows="9" aria-label="Editable generated study prompt"/><div className="row-actions"><button className="button primary" type="button" onClick={launch}>Open {provider==="gemini"?"Gemini":"ChatGPT"} ↗</button><button className="button" type="button" onClick={copy}>{copied?"Copied ✓":"Copy only"}</button></div><small>Paste the copied prompt into the conversation that opens. Snoozelet never sends your data automatically.</small></section>;}
