import { requireUser } from "@/lib/auth";
import { getMotivationSummary,getRecentReviews,getReviewTotals } from "@/lib/db";
export const dynamic="force-dynamic";
export default async function ProgressPage(){const user=await requireUser();const [totals,recent,summary]=await Promise.all([getReviewTotals(user.id),getRecentReviews(user.id,20),getMotivationSummary(user.id)]);return <main className="workspace-page"><header className="workspace-header"><div><p className="eyebrow">Progress</p><h1>What you know and what needs work</h1><p>Use these learning signals to choose the next session.</p></div></header><section className="learning-metrics"><article><span>Accuracy</span><strong>{totals.accuracy}%</strong><small>{totals.total} recorded answers</small></article><article><span>Weak cards</span><strong>{totals.weak_cards}</strong><small>need another retrieval</small></article><article><span>Reviewed today</span><strong>{summary.todayReviews}</strong><small>active recall attempts</small></article><article><span>Reviewed this week</span><strong>{summary.weeklyReviews}</strong><small>{summary.weeklyReviews?Math.round(summary.weeklyCorrect/summary.weeklyReviews*100):0}% correct</small></article></section><section className="progress-section"><h2>Deck mastery</h2><div className="mastery-list">{summary.mastery.map((deck)=><article key={deck.id}><div><strong>{deck.title}</strong><span>{deck.mastered} of {deck.cards} mastered</span></div><div className="goal-track"><span style={{width:`${deck.percent}%`}}/></div><b>{deck.percent}%</b></article>)}</div></section><section className="recent-panel" id="history"><h2>Recent answers</h2>{recent.map((review)=><article key={review.id}><span className={review.correct?"review-dot correct":"review-dot"}/><div><strong>{review.term}</strong><small>{review.deck_title} · {review.mode}</small></div><b>{review.correct?"Correct":"Review"}</b></article>)}</section><section className="progress-section backup-panel" id="backup"><div><h2>Protect your study data</h2><p>The SQLite backup is a complete copy. Portable JSON is useful for inspecting or migrating the contents.</p></div><div className="row-actions"><a className="button" href="/diagnostics">Storage details</a><a className="button" href="/api/backup">Portable JSON</a><a className="button primary" href="/api/backup/local">Download SQLite backup</a></div></section></main>}
